# TODO

## Rebrand: Macro Tracker → Macra

- [x] Renamed the app "Macra", tagline "Track Your Macros" — updated everywhere user-facing: `<title>`/meta description (`src/app/layout.tsx`), the shared auth-page wordmark (`src/app/(auth)/layout.tsx`, now logo+name with the tagline underneath instead of just the name), the desktop sidebar wordmark (`src/components/nav/side-nav.tsx`), and `README.md`'s title. Verified live: rendered HTML confirmed the new title/tagline and zero remaining occurrences of "Macro Tracker"
- [x] New accent color: muted sage green `#3e8967` (`--accent` in `globals.css`), replacing iOS blue everywhere it's used (buttons, links, active nav state, focus rings, segmented control indicator) — one variable, so it propagates everywhere automatically. Deliberately left the macro data-viz colors (protein/carbs/fat/calories/calories-over) untouched — those carry specific meaning independent of brand color, changing them wasn't asked and would break the established visual vocabulary
- [ ] **Not touched, worth knowing about**: `src/app/favicon.ico` is still Next.js's unbranded default (a real icon needs actual image asset work, not just a code edit); the MCP server's registered name (`mcp-server/index.ts`, `"macro-tracker"`) and `.mcp.json`'s key still say "macro-tracker" — left alone since renaming it would require reconnecting/reconfiguring the MCP client, and that's an internal technical identifier, not user-facing branding; `package.json`'s `"name"` field is likewise still `"macro-tracker"` (npm/internal only, no user visibility)

## Scaling to multiple users (audit, 2026-09-07 — before sharing with friends)

Not yet implemented, just findings — user asked for a heads-up before inviting friends to use the app for real. Ranked by what actually blocks/hurts sharing, not file order.

- [ ] **Not deployed anywhere yet** — only runs via `npm run dev` on localhost. Friends can't reach it at all until it's hosted somewhere public (Vercel is the natural fit for Next.js; DB is already on Neon). This is the actual blocker before anything else here matters
- [ ] **Gemini free-tier quota (20 requests/day) is shared across the entire app, not per-user** — confirmed via the real 429 hit earlier this session. With even 2-3 friends trying "Describe it"/photo estimate, the quota exhausts almost immediately and everyone sees the same failure regardless of who used it up. Either budget for the (cheap, at this volume) paid tier before sharing, or accept/communicate that AI estimate is a rare-use feature for now. USDA's key (~3600/hr, also shared) is much less likely to be a real problem at friend-group scale
- [ ] **No rate limiting/abuse protection anywhere** (register, login, estimate endpoints) — already tracked under Auth above, but matters more once the app is reachable by more than one trusted person
- [ ] **MCP server is single-user by design, not multi-tenant** — `mcp-server/index.ts` resolves one fixed account via `MCP_USER_EMAIL` at process startup. If the intent is for friends to also log food by talking to Claude (not just the web UI), each of them needs their own local MCP server config pointed at their own account — it does not automatically support multiple people through one shared setup
- [ ] **DB latency (neon-http, no connection pooling, ~795ms/route observed solo) will compound under concurrent multi-user load** — already an open, deferred item; worth prioritizing once more than one person is actually hitting it at the same time
- [x] **Data isolation is already correct** — verified `users`/`foods`/`recipes`/`foodEntries`/`macroGoals` all carry real per-user `userId` FKs, custom foods are correctly scoped to their owner in search results, and no server secret (`GEMINI_API_KEY`/`USDA_API_KEY`/`JWT_SECRET`/`DATABASE_URL`) is referenced anywhere in client-side code. The auth/session mechanism itself (JWT, httpOnly/secure/sameSite cookies, bcrypt) is genuinely multi-tenant already — nothing here is hardcoded to one user
- [ ] No password reset or email verification (see Auth section) — low-stakes for a small friends-only app, but a forgotten password has no self-service recovery today

## Auth

- [x] Email/password auth with JWT sessions (register, login, logout, `/api/auth/me`)
- [x] **Google OAuth ("Continue with Google")** — first item off the scaling audit above. Hand-rolled Authorization Code + PKCE flow (`src/lib/auth/google.ts`) rather than pulling in a library (NextAuth/arctic/etc.) — the app already has `jose` for JWTs, so verifying Google's id token against its live JWKS (`createRemoteJWKSet`, not just decoding it unverified) needed no new dependency, and it plugs into the exact same `createSession()`/`mt_session` cookie every other login path already uses, so nothing downstream (goals, entries, MCP) had to change. `GET /api/auth/google` generates CSRF `state` + a PKCE pair, stashes them in short-lived cookies scoped to `/api/auth/google` only, and redirects to Google; `GET /api/auth/google/callback` verifies `state`, exchanges the code, verifies the id token, then finds-by-`googleId` → finds-by-`email` (links an existing password account rather than creating a duplicate) → creates new, in that order
- [x] Schema: `users.passwordHash` is now nullable (a Google-only account never sets one — login explicitly checks for null before ever calling `bcrypt.compare`, rather than passing null into it), plus a new nullable-but-unique `users.googleId`. Applied directly via SQL rather than `drizzle-kit push` — push wanted an interactive confirm for the new UNIQUE constraint (Postgres allows multiple NULLs under a unique constraint, so this was always safe with the table's 1 existing row; drizzle-kit is just cautious by default) and this session's shell is non-interactive, so `--force` was the alternative but got blocked by the environment's own auto-mode classifier as too destructive-sounding — ran the exact two `ALTER TABLE` statements by hand instead, then confirmed `drizzle-kit push` reports "No changes detected" afterward
- [x] "Continue with Google" / "Sign up with Google" button (`src/components/auth/google-button.tsx`, Google's actual multi-color "G" mark — required by their branding guidelines, unlike this app's own generic-AI-icon choice elsewhere) added to both `/login` and `/register`, above a divider over the existing email/password form — neither form changed
- [x] Verified everything testable without a real registered Google OAuth client/live consent screen: the init route's PKCE/state generation and constructed authorization URL (direct function call, correct `client_id`/`redirect_uri`/`scope`/`code_challenge`/`prompt` params); the friendly `/login?error=` redirect when `GOOGLE_CLIENT_ID`/`SECRET` are unset; a password-login attempt against a hand-inserted Google-only test user (`password_hash: null`) correctly 401s instead of crashing; registering with an email that already has a Google-linked account correctly 409s instead of creating a duplicate. Test rows cleaned up after each check
- [x] **Live-tested by the user**: set real `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` in `.env.local`, dev server restarted to pick them up (killed the old process, `npm run dev` fresh — confirmed "Environments: .env.local" + a real Google authorization URL with the actual client id on the next `/api/auth/google` hit), then clicked through the real consent screen end-to-end. Confirmed working
- [x] **Usernames** — `users.name`, a free-typed display name (not a unique handle). Defaults to Google's `given_name` claim on Google sign-up, or a required "Name" field on the email/password register form; editable anytime from Settings (`PUT /api/auth/me`). `GET /api/auth/me` now does a real DB read for `name` (id/email still come straight from the JWT) instead of returning session-only data, since a Settings edit needs to show up without forcing a re-login. Schema migration: added nullable, backfilled the two existing real rows from their email's local part (capitalized) since neither had a name yet, then set `NOT NULL` — direct SQL again, same reasoning as the OAuth migration above
- [x] **Greeting + welcome messages**: Today's header now reads "{Good morning/afternoon/evening}, {first name}" instead of a plain "Today" label (`greetingForTime()`/`firstName()` added to `mock-data.ts`, same file `mealForTime()` already lived in). Email/password register shows `Welcome, {name}!` immediately client-side; Google sign-up can't do that (it's a server redirect, no client JS in between) so the callback redirects to `/?welcome=1` only when it actually created a brand-new account (never on a returning-user login or an existing-account link), and Today shows the toast once `user` has loaded, then cleans the query param
- [x] Verified live: register with a name → 201 with `name` in the response; `GET/PUT /api/auth/me` round-trips a name change correctly; registering without a name → 400 (zod rejects `undefined`); typecheck/lint clean project-wide (two pre-existing, unrelated lint warnings on effects this change didn't touch — Today's `loadEntries` and `AuthProvider`'s own `refresh()`). Test accounts cleaned up after
- [ ] The two real existing accounts (yours + one other — `swaggykk20@gmail.com`, presumably already tested Google sign-in too) both got the email-derived placeholder name ("Karlyolarte20"/"Swaggykk20") from the backfill, not a real first name — Google's `given_name` only gets pulled in on a fresh account *creation*, not when linking to an already-existing row. Worth renaming both in Settings
- [ ] OAuth providers beyond Google (Apple, etc.) — not requested, not built
- [ ] Password reset flow (forgot password email)
- [ ] Email verification on register
- [ ] Rate limiting / lockout on `/api/auth/login` and `/api/auth/register`
- [x] Gate app routes behind auth — `src/app/(app)/layout.tsx` redirects to `/login` server-side if there's no session (not `proxy.js`/`middleware.js`; a Server Component redirect was simpler for gating one route group). The app now requires an account to use at all
- [x] Scope food entries + macro goals to the logged-in user — done as part of Persistence below

## Persistence

- [x] `food_entries` table — snapshots its own macros at log time (like a custom food) with an optional `food_id` back-reference, plus a real `logged_at` timestamp the old `localStorage` entries never had (there was no way to tell "today" from any other day)
- [x] `macro_goals` table — one row per user
- [x] API routes: `GET/POST /api/entries` (list for a date / create), `DELETE /api/entries/[id]`, `GET /api/entries/summary?days=` (per-day totals for History), `GET/PUT /api/goals`
- [x] Rewired the Today page, Settings goals form, and History page off `localStorage` onto these routes; deleted `use-local-storage.ts` and the now-empty `mock-data.ts` seed exports (`INITIAL_ENTRIES`, `HISTORY`) since nothing reads them anymore
- [x] Verified live end-to-end: add/list/delete entries, history summary math, goals upsert, and cross-user isolation (a second user sees empty entries + default goals, and can't delete another user's entry — 404, not leaked)
- [ ] Known simplification: "today"/day boundaries use the client's local calendar date but are stored/queried as UTC-day boundaries server-side — no per-user timezone is tracked anywhere yet, so this can be off by a few hours near midnight for users far from UTC
- [ ] History's per-day goal comparison always uses the user's *current* goal, not whatever it was on that historical day (goals aren't versioned) — same simplification the old mock data had

## Food & nutrition database

- [x] `foods` table unifying USDA FoodData Central, Open Food Facts, and user-created custom foods (per-100g/ml normalized, cached by `source`+`external_id`)
- [x] Food search API (`GET /api/foods/search?q=`) — queries USDA + Open Food Facts, normalizes, caches in `foods`, merges with custom foods scoped to the logged-in user
- [x] Custom food creation (`POST /api/foods/custom`, auth-required) — entered per-serving, converted to per-100g on save
- [x] Skip external API calls on a cache hit — macros don't change, so once a query has cached USDA/OFF matches, `/api/foods/search` serves purely from the DB instead of re-fetching
- [x] **Fixed the cache-skip check to be per-source, not one combined flag**: found live — searching "banana" had 13 cached Open Food Facts rows and zero USDA rows, but the skip check (`cached.some(row => row.source !== "custom")`) treated *any* cached non-custom match as "we've got this query covered," so USDA was silently never queried at all, not even once `DEMO_KEY` resets. Whichever source responded first permanently "poisoned" the skip for the other. Fixed by tracking `hasCachedUsda`/`hasCachedOff` separately and only substituting `Promise.resolve([])` for the source that's actually already covered. Verified live: re-searching "banana" now shows `warnings: ["USDA search unavailable"]` — USDA is genuinely being attempted again — while OFF still correctly skips its already-cached results
- [x] **Got a real `USDA_API_KEY`** — registered free at api.data.gov, added to `.env.local`, dev server restarted to pick it up. Verified live end-to-end: a fresh query returned real USDA results with no "unavailable" warning, and a direct rate-limit header check showed `X-Ratelimit-Limit: 3600` (not the 1,000/hour USDA's own docs state — same doc-vs-live discrepancy pattern as `DEMO_KEY`'s documented 30/hr vs. the observed 10/hr; trust the live header). Along the way: the key briefly ended up in `.env.example` instead of `.env.local` — confirmed it was never git-tracked (gitignored via `.env*`, `git ls-files` showed it untracked) so no leak occurred, moved it to the right file and cleared the placeholder
- [x] Removed the static `QUICK_ADD_SUGGESTIONS` list — the Add Food sheet now suggests recently-logged foods instead, deduped by name from the user's own entries
- [x] **Wired the Add Food sheet to `/api/foods/search`** — extracted the debounced fetch logic out of `FoodSearchInput` into a shared `useFoodSearch()` hook (`src/components/food-search/use-food-search.ts`) so both it and the Add Food sheet search identically. Typing 2+ characters now shows real USDA/OFF/custom results instead of filtering the recent-foods chips; the chips still show first when the box is empty (the zero-effort "log what I always eat" path stays, search is for finding anything else)
- [x] Selecting a real search result scales its per-100g/ml macros to that food's own serving size (or 100 if it has none) and fills the form — verified live the round-trip is exact (a 300g/220kcal custom food scaled back to exactly 220/25/8/9)
- [x] **Fixed word-order search bug**: searching "adobo chicken" didn't match "Chicken Adobo" because the whole query was matched as one literal substring. Now splits the query into words and ANDs a separate `ILIKE '%word%'` per word — order and adjacency no longer matter. Applied to both `/api/foods/search` and `GET /api/recipes?q=`, verified live (reversed order, non-adjacent words, and mixed case all match correctly). Case was never actually the problem — `ILIKE` is already case-insensitive; lowercasing the stored `name` was considered and rejected since it would've thrown away real display casing for zero search benefit
- [x] **`pg_trgm` GIN index + typo-tolerant fuzzy search** — enabled the `pg_trgm` extension on Neon, added `gin_trgm_ops` GIN indexes on `foods.name` and `recipes.name` (accelerates the `ILIKE` matching from the word-order fix too). Each query word matches on exact `ILIKE` substring OR a pg_trgm fuzzy match — a typo like "chiken" still finds "Chicken Adobo"
- [ ] The fuzzy branch isn't index-accelerated yet — the GIN trgm index matches on raw `name`, but the query lowercases both sides for case-insensitivity, so an expression index on `lower(name)` would be needed to speed up that specific branch. Fine at current scale (the `ILIKE` branch, which is accelerated, already narrows most real queries)
- [x] **Real bug found and fixed: fuzzy search was returning unrelated foods** — user reported searching "banana" in the recipe ingredient picker surfaced Chicken Breast, Salmon Fillets, and Adobo. Root cause: the original fuzzy check used `word_similarity(word, whole_name_or_brand)` at threshold 0.3 — comparing a short word against an *entire* multi-word string. "banana" has heavy internal letter repetition (an/na/ana), giving it very few distinct trigrams, so matching just one or two common ones against a long unrelated string was enough to score above 0.3 (verified: 0.33-0.5 against completely unrelated text). Rewrote to compare the query word against each *individual* word of name/brand instead (`similarity()`, word-vs-word, via `unnest(string_to_array(...))`), and raised the threshold to 0.4 — verified against real data this blocks all 7 observed false positives (they score 0.07-0.14, occasionally 0.3 for a visually-similar word) while still catching the most common typo patterns (missing a letter, adjacent-key slip both score 0.5+). Applied to both `/api/foods/search` (name + brand) and `GET /api/recipes?q=`
- [ ] **Known, disclosed tradeoff of the 0.4 threshold**: rarer typo patterns (transpositions, removing a middle letter — e.g. "adbo" for "adobo" scores 0.375, just under the cutoff) can now slip through uncaught, where the old looser threshold would have caught them — at the cost of also catching every false positive like the banana case. Chose fewer false positives over maximum typo recall; not revisiting unless it proves too strict in practice
- [x] **Search now also matches `foods.brand`, not just `name`** — found while checking Filipino canned-goods brand coverage: some Open Food Facts products don't repeat their brand in the name at all (`product_name: "Corned Beef"`, `brand: "Argentina"`), so name-only search silently missed brand-only queries. Each word now matches name OR brand, exact-or-fuzzy, same logic as before. Verified live: brand-only ("argentina"), mixed brand+name ("argentina beef"), and a typo'd brand ("argentna") all correctly find it; confirmed no crash/false-positive from foods with a null brand
- [x] Confirmed live (not assumed) that major Filipino canned-goods brands exist on Open Food Facts: Ligo (19 products), Purefoods (135), Century Tuna (70), Mega Sardines (27), Argentina (14). USDA has none of these — it's US-only. Two brands (555, CDO) couldn't be confirmed either way — OFF rate-limited the check (503s) before an answer came back, not necessarily absent
- [x] **Root-caused a "coverage looks missing" report that wasn't a coverage problem**: user couldn't find "pancit canton" — turned out OFF has 68 matching products and our search finds them fine when the APIs aren't down. The real issue: `useFoodSearch` read `data.results` but silently dropped `data.warnings`, so a temporary USDA/OFF failure (very common this session from heavy testing) rendered the exact same "No matches found" as a genuine zero-result query, with no way to tell the two apart
- [x] **Fixed**: `useFoodSearch` now returns `warnings` alongside `results`; both `FoodSearchInput` and the Add Food sheet show a distinct message ("X unavailable — try again in a moment", or "— results may be incomplete" if some results still came back) instead of the generic empty state whenever a source actually failed. Verified against a real live failure (both USDA and OFF happened to be down at the same time from today's testing) rather than a simulated one
- [ ] Barcode scanning UI — `getOpenFoodFactsByBarcode()` already exists in `src/lib/food-sources/openfoodfacts.ts`, just unused
- [x] **Food entry ↔ catalog linking now actually happens**: selecting a real search result in the Add Food sheet tracks its id (`selectedFoodId`), cleared if the name is hand-edited afterward, and passes it through as `food_id` on submit. "Recently logged" chips carry it forward too, since entries returned from the API already include `foodId`. Verified live — `food_entries.food_id` is populated for the first time, no longer always `null`
- [x] **Cleared the entire USDA/OFF cache** (519 rows: 282 USDA + 237 OFF) plus the 28 `searched_queries` records for those sources — checked first that nothing would break: zero recipe ingredients referenced any of them (would have cascade-deleted silently, `onDelete: "cascade"` on `recipe_ingredients.food_id`) and zero diary entries either (those just `set null`, harmless, since entries already snapshot their own macros). `foods` table now empty; going forward it only grows from custom foods / `create_custom_food` / recipe-estimate ingredients
- [x] **Added `DISABLE_EXTERNAL_FOOD_APIS` toggle** (`src/lib/food-sources/search-foods.ts`) — set to `"true"` in `.env.local` to skip USDA/OFF entirely and search only the app's own `foods` table. Implemented by forcing the existing per-source cache-skip flags to `true` unconditionally, reusing the exact code path that already substitutes an empty result instead of calling out, rather than adding a parallel disabled-mode branch. Verified live: a fresh "chicken breast" search that would normally hit both APIs now returns in 0.34s with zero results (DB is empty) and zero warnings; confirmed real (custom) foods are still found correctly, so this isn't just returning empty regardless of DB state. Required restarting the dev server (`.env.local` only read at process start) — found and killed the specific process on port 3000 rather than guessing
- [x] **Fixed properly, not just the earlier stopgap** — user caught the "1 pcs" stopgap looking broken in practice: re-tapping a recent chip for an *already-linked* food showed a blank/zero-looking Amount, which is confusing even though it didn't affect what actually got logged. Root cause: recent chips only ever store a freeform serving string, never structured amount data, and there was no way to recover a real number for an already-linked food. Added `GET /api/foods/[id]` (same public-vs-custom visibility rule as search) so `applySuggestion` can now fetch the linked food's own `servingSize`/`baseUnit` and show that instead of blank — deliberately without touching the entry's own recorded calories/protein/carbs/fat/servingLabel, since a recent chip means "repeat exactly what I ate," not "re-derive from the catalog's current numbers." Unlinked chips still default to `amount: "1", unit: "pcs"` as before. Verified live: created a real custom food (`servingSize: 2, servingUnit: "pcs"`), confirmed the new route returns it correctly to its owner (200), 404s for a non-owner (no leak) and for a nonexistent id

## Recipes

- [x] `recipes` + `recipe_ingredients` tables — user-owned only (no public/shared recipes, no external recipe API), ingredients reference `foods` by id + amount, macros computed on read (sum of ingredients ÷ servings) rather than stored, so they can't go stale
- [x] `POST /api/recipes` (create, auth-required), `GET /api/recipes?q=` (list/search own recipes with computed macros), `GET /api/recipes/[id]` (single, with full ingredient breakdown)
- [x] Privacy guard: an ingredient can reference any public USDA/OFF food, but a `custom` food only by its own owner — verified live (403 on a cross-user attempt)
- [x] Decided against merging into `/api/foods/search` — recipes get their own dedicated tab instead of unified search results
- [x] `DELETE /api/recipes/[id]` — added once the UI actually needed it (list/detail pages)
- [x] **Recipes tab built**: new `/recipes` (list, create via a bottom sheet / desktop dialog matching the Add Food sheet's pattern) and `/recipes/[id]` (ingredient breakdown, per-serving totals, delete) — added as a 4th nav destination in both `SideNav` and `TabBar`
- [x] Reusable `FoodSearchInput` (`src/components/food-search/`) — debounced (350ms, min 2 chars), race-safe (ignores stale in-flight responses), used for the recipe ingredient picker; built generically so the Add Food sheet's search box can reuse it later instead of duplicating
- [x] "Quick log" closes the loop to entries: recipe detail page logs 1 serving's computed macros to `/api/entries` for a chosen meal — verified live
- [x] Add Food sheet's search box is now wired to the same search logic (via the shared `useFoodSearch` hook) — see Food & nutrition database section above
- [ ] No recipe update/edit route yet — only create, list, and delete
- [ ] No way to log more than 1 serving of a recipe at once from the detail page
- [x] **Fixed a real crash**: an Open Food Facts product name (`Meritene Forza e Vitalità`, barcode `8445290333346`) contained an embedded NUL byte from an upstream OFF encoding issue — Postgres text columns reject NUL bytes outright, so the insert threw, the whole `/api/foods/search` request 500'd, and the client's `res.json()` on that broken response is what actually surfaced as "Unexpected end of JSON input" in `FoodSearchInput`. Added `sanitizeText`/`sanitizeNullableText` (`src/lib/food-sources/sanitize.ts`) applied to every external name/brand/serving-label field in both the USDA and OFF normalizers. Also hardened `upsertFoods` to use `Promise.allSettled` instead of `Promise.all` — one malformed item from messy crowdsourced data now gets logged and skipped instead of failing the entire search. Verified live: the exact previously-failing insert now succeeds (with the corrupted characters just gone — "Vitalità" → "Vitalit", "Nestlé" → "Nestl" — since the original bytes are unrecoverable, but it no longer crashes)
- [x] Also fixed `FoodSearchInput` itself to not propagate an unhandled rejection when a search fails for any reason — added a proper `catch` (previously only had `try/finally`)
- [x] That fix first went through an `AbortController` that actively cancelled a superseded in-flight request — reverted after it produced its own `AbortError: signal is aborted without reason` unhandled rejection in practice, root cause not fully pinned down. Reverted to the simpler `cancelled` flag (ignore stale results, let the request finish) — fewer edge cases, only cost is an occasional wasted in-flight request that gets silently ignored
- [x] **Fixed an N+1 query in the recipe list** (`GET /api/recipes`): was fetching each recipe's ingredients with a separate query per recipe. Added `getRecipeIngredientDetailsBatch()` — one query for every listed recipe's ingredients at once, grouped in code. Neon's `neon-http` driver has no connection pooling (~250-350ms per query, confirmed by measuring a non-DB route against a DB-touching one), so this matters more as recipe counts grow, even though the measured improvement at only 6 recipes was small (concurrent queries already parallelize decently once Neon is warm) — see [[Performance & Optimizations]] in Obsidian for the full latency writeup
- [x] **Added "Describe it" mode to the recipe sheet too** (`src/lib/llm/estimate-recipe.ts`, `POST /api/recipes/estimate`) — unlike Add Food's estimate (one aggregate macro estimate), a recipe needs real decomposed ingredients since that's how recipes are stored. Describing a dish (e.g. "peanut butter sandwich with 2 slices of bread and 2 tbsp peanut butter") calls Gemini for a structured `{name, servings, ingredients[]}` breakdown, then creates a real custom food per ingredient (`createCustomFood`, extracted from `POST /api/foods/custom` for reuse) so the ingredient list can reuse the exact same `FoodSearchResult`-shaped UI/math the sheet already had for search-picked ingredients — no second parallel ingredient representation
- [x] Extracted shared Gemini plumbing (`src/lib/llm/client.ts`, `src/lib/llm/retry.ts`) once a second LLM call existed, so `estimate-macros.ts` and `estimate-recipe.ts` share the client singleton and the 429/503 retry-with-backoff instead of duplicating it
- [x] Verified live end-to-end, not just the estimate step in isolation: described the app's own original "peanut butter sandwich" example, confirmed the two decomposed ingredients (bread 2 slices, peanut butter 2 tbsp) came back with correct per-100 conversions, then actually created the recipe from them and confirmed the summed macros (330 kcal) matched the ingredients exactly. Also regression-tested `POST /api/foods/custom` after extracting `createCustomFood` out of it. All test data cleaned up afterward

## Public foods & recipe visibility

- [x] **Custom foods are now public/shared across all users**, not scoped to their creator — matches how USDA/OFF's own cached rows already behaved. Removed the `or(ne(source,'custom'), eq(userId, userId))` filter from `search-foods.ts` entirely; the function no longer takes a `userId` parameter at all (nothing left to scope by), so `GET /api/foods/search` dropped its `getSession()` call and the MCP `search_food` tool call site simplified too. `foods.userId` stays on the table as creator attribution, just no longer an access-control field (schema comment updated to say so explicitly)
- [x] Removed the now-obsolete recipe-ingredient privacy guard (`RecipeIngredientForbiddenError`, the `hasInaccessibleFood` check in `create-recipe.ts`) — a recipe can reference any food's id since all foods are public now; only existence is still checked (`RecipeIngredientNotFoundError` stays). Cleaned up the now-dead catch branches in `POST /api/recipes` and the MCP `create_recipe` tool
- [x] **"Can't find it, try asking AI" prompt** added to the empty-search-results state in both places a food search can come up empty: `FoodSearchInput` (new optional `onAskAi` callback prop — used by the recipe ingredient picker) and Add Food sheet's own inline search UI (which duplicates the search box rather than using `FoodSearchInput`, so needed its own copy of the button). Both flip the parent's existing `mode` state to "describe" and pre-fill the description textarea with whatever was typed in the search box, rather than starting the AI flow from a blank field. Text is intentionally the literal string requested — user said they'll swap it for a mascot name later, not asking for a config system now
- [x] **Recipes: private by default, owner-toggleable public** — new `recipes.isPublic` boolean column (`default(false)`), pushed live as a purely additive column (no confirmation prompt needed, unlike the two earlier UNIQUE-constraint migrations this session). `createRecipeSchema` gained `isPublic` (defaults false); `CreateRecipeSheet` gained a Private/Public `SegmentedControl` next to Servings, reset to Private whenever the sheet reopens
- [x] `GET /api/recipes` (list) now returns the requester's own recipes (any visibility) **plus every other user's public recipes** — `or(eq(userId, session.sub), eq(isPublic, true))`, joined against `users` for `ownerName`, with a computed `isOwner` per row so the UI can gate edit/delete. `GET /api/recipes/[id]` (detail) now fetches by id alone first, then checks visibility in code (`row.userId !== session.sub && !row.isPublic` → 404) instead of AND-ing ownership into the query itself — previously there was no way to fetch a recipe you didn't own at all, even a would-be-public one, since the WHERE clause excluded it outright
- [x] **New `PATCH /api/recipes/[id]`** — the actual toggle endpoint, owner-only (403 for a non-owner, even though they can already view a public recipe — no info leaked since they already know it exists). `DELETE` stays owner-only and unchanged: a public recipe can be viewed/quick-logged by anyone, but only its creator can edit visibility or delete it
- [x] Recipes list (`/recipes`) shows a small globe icon next to a public recipe's name, plus "by {ownerName}" in the meta line for anything not your own. Recipe detail page shows "by {ownerName}" under the title for non-owners, gates the delete button to `isOwner`, and — owner-only — adds a new "Visibility" card with the same Private/Public toggle, optimistically updated with rollback-on-failure
- [x] MCP `create_recipe` tool gained a matching optional `isPublic` input (defaults false, same as the web form); `create_custom_food`'s description text updated to stop claiming results are "(scoped to you)" now that they aren't
- [x] **Verified live end-to-end with two real throwaway accounts** (not just unit-level reasoning): Alice created a custom food, confirmed Bob (and an unauthenticated caller) could find it via search; Bob created a *private* recipe referencing Alice's food (would have 403'd before this change) and confirmed Alice's list didn't include it and direct fetch 404'd; Bob flipped it public via the new PATCH route and confirmed Alice's list then showed it with `isOwner: false, ownerName: "Bob"`; confirmed Alice could view the now-public detail but got a real 403 trying to PATCH its visibility and a 404 (not a leak, not a success) trying to DELETE it, then confirmed via a direct fetch as Bob that the recipe genuinely still existed — the delete attempt was actually rejected, not silently misreported. All test accounts/data cleaned up afterward (cascade-deleted cleanly, verified zero orphaned rows)
- [ ] No "Discover" or browse-by-popularity view for public recipes — they only surface by being in the same list as your own (or by name search). Fine for a friends-sized group; would need its own UI if the public catalog grows large
- [ ] No moderation/reporting path for public content (foods or recipes) — anyone can create a public food/recipe visible to everyone, with no review step. Low-risk for a small trusted friend group, worth reconsidering if the app opens up further

## MCP

- [x] **v1 MCP server built and verified live** (`mcp-server/index.ts`, wired into Claude Code via `.mcp.json`) — exposes `search_food` and `log_entry` over stdio, so an AI assistant can search foods and log entries by conversation instead of the Add Food sheet. Single-user app, so it deliberately skips real per-request auth: runs as its own local process, imports the DB and search logic directly (no HTTP round-trip through Next.js), and acts as one fixed account resolved once at startup from `MCP_USER_EMAIL`
- [x] Extracted `/api/foods/search`'s full search logic (word matching, fuzzy typo tolerance, the `searched_queries` cache-skip, unbranded-first sort) into `src/lib/food-sources/search-foods.ts` so the MCP tool and the REST route share identical behavior instead of duplicating it — the route is now a thin wrapper around it
- [x] Verified live end-to-end with a real MCP client (spawned the server, called both tools over the actual stdio protocol, not just unit-tested the handlers): `search_food("chicken breast")` returned real results matching the web UI; `log_entry(...)` inserted a real row under the correct user account, confirmed via direct DB query, then cleaned up
- [x] **Added a third tool, `estimate_macros`** (Gemini, not Claude — chosen for its free tier since this app's LLM volume is too low to justify paying per call) — an LLM fallback for foods USDA/OFF genuinely don't have (home-cooked dishes). Verified live: real structured estimates for "sinigang na baboy" and "2 fried eggs with rice"; also hit a real 503 from Gemini's free tier under load (not a bug, a known free-tier characteristic — no retry logic yet, deliberately deferred). Doesn't cache into `foods` — an LLM guess isn't shared-cache-worthy the way real USDA/OFF data is
- [x] Extracted `estimateMacros` into `src/lib/llm/estimate-macros.ts` so the MCP tool and a new web route share it, same pattern as `search-foods.ts`
- [x] **Wired into the actual web UI, not just MCP** — Add Food sheet has a "Search" / "Describe it" toggle; describing a food calls the new auth-required `POST /api/foods/estimate` and fills the same editable macro fields search results already use, showing the estimate's confidence + assumptions inline. Verified live through real auth (register → estimate → 200; unauthenticated → 401)
- [x] **Added retry-with-backoff for Gemini free-tier 503s/429s** (`src/lib/llm/estimate-macros.ts`) — retries up to 2 more times (500ms, then 1500ms delay) on `ApiError` with status 429/503, both transient; anything else (bad request, auth, model-not-found) fails immediately since retrying won't fix it. Verified live: happy path still works correctly after the change
- [x] **Added a fourth tool, `create_recipe`** — extracted `POST /api/recipes`'s creation logic (the cross-user privacy guard, the neon-http no-transactions workaround) into `src/lib/recipes/create-recipe.ts` so the REST route and the MCP tool share it identically, same pattern as `search-foods.ts`/`estimate-macros.ts`. Verified live via a real MCP client: `search_food` → `create_recipe` with two real ingredient ids → correct computed macros, confirmed via direct DB query under the real account, then cleaned up. Also regression-tested the REST path itself (register → search → `POST /api/recipes` → 201 with correct macros) to confirm the extraction didn't break the web UI's own recipe creation
- [ ] Only `search_food` + `log_entry` + `estimate_macros` + `create_recipe` exist — no `log_recipe` (logging a recipe serving to today's diary) yet, no `get_today_summary`/`get_goals` read tools, no custom-food creation via MCP
- [ ] No refresh/TTL on `searched_queries` — an already-searched query (e.g. via MCP or the web UI) never re-asks Open Food Facts even as its crowdsourced catalog grows over time
- [x] **Added photo input to `estimate_macros`** (Add Food sheet's "Describe it" mode) — lets you photograph a nutrition label/packaging instead of only describing in text. Real accuracy difference, not cosmetic: text-only "sugar free peanut butter from Mood Food" got a generic guess (model's own notes admitted it didn't know the brand); a label photo lets Gemini read the actual printed values. Verified against a real, known-value image (the FDA's own official example nutrition label, downloaded from Wikimedia Commons) — every number matched exactly (230 kcal, 3g protein, 37g carbs, 8g fat, "2/3 cup (55g)" serving), with `confidence: "high"` correctly reflecting it read real data rather than guessed. `estimateMacros()` now takes an optional `image: {data, mimeType}` alongside the optional text description (at least one required); the route caps the base64 payload at 10MB, generously above a typical phone photo
- [x] **Brought image support to the MCP `estimate_macros` tool too** — it previously only accepted text, while the web UI already had the camera button; added optional `imageBase64`/`imageMimeType` params, `description` made optional (at least one of the two required), so a Claude conversation with an attached product photo gets the same real-label-reading behavior as the web app. Verified live through the actual MCP protocol with the same known-value FDA label image used for the web route — identical correct result (230 kcal, 3g protein, 37g carbs, 8g fat, high confidence); also verified the "neither provided" case returns a clean `isError` instead of crashing
- [x] **Fixed the above inconsistency, plus added a fifth tool, `create_custom_food`** — the user's actual ask ("add a searchable food to the database, not log it") needed `MacroEstimate` to carry a real structured serving, not just a display string, so both fixes share one root change: `estimateMacros()` now also returns `brand: string | null`, `servingSize: number`, `servingUnit: "g"|"ml"|"pcs"` alongside the existing `servingDescription` text (Gemini schema + system instruction updated to require them, e.g. "2 fried eggs" → `servingSize: 2, servingUnit: "pcs"`). Purely additive — didn't break the existing `MacroEstimate` consumers
- [x] With real structured data available, `applyEstimate` in the Add Food sheet now sets `form.amount`/`form.unit` from it instead of leaving amount blank — this was silently skipping the custom-food-save gate in `handleSubmit`, so single-food "Describe it" estimates now save as reusable custom foods too, same as recipe ingredients already did. Verified live: "2 fried eggs" → `servingSize: 2, servingUnit: "pcs"` → simulated the fixed submit flow end-to-end → `POST /api/foods/custom` 201
- [x] New MCP tool `create_custom_food` — explicitly for adding a searchable catalog entry from a description/photo, distinct from `estimate_macros` (returns an estimate to log, doesn't persist) and `log_entry` (logs to the diary, doesn't create a catalog entry). Composes `estimateMacros()` + the existing `createCustomFood()` (already shared with `POST /api/foods/custom` and the recipe-estimate route). Verified live through the real MCP protocol with the same known-value FDA label image used throughout this feature: created a real custom food with `servingSize: 55, servingUnit: "g"`, confirmed the per-100 conversion is exact (418.18/100×55 = 230 kcal, matching the real label), then confirmed it's immediately findable via `search_food` — closing the loop the user actually asked for. Test data cleaned up
- [x] Made estimate failures debuggable instead of one generic message — `POST /api/foods/estimate` and `POST /api/recipes/estimate` now surface the real error (`src/lib/llm/error-message.ts`: Gemini's actual status + message via `ApiError`, or our own thrown error text) instead of always "Couldn't get an estimate right now." Client already displayed `data.error` directly, so no UI change needed
- [ ] **BLOCKED for the rest of 2026-09-07, retry 2026-09-08**: the very first live test with the new error message immediately revealed the real cause of the session's flaky estimate failures — the Gemini key has hit its **free-tier daily quota: 20 requests/day for `gemini-3.6-flash`** (`generativelanguage.googleapis.com/generate_content_free_tier_requests`, confirmed via a real 429 response, not assumed). All of `estimate_macros`, `estimate_recipe`, `create_custom_food` (image or text) are unusable until this resets. Everything built after this point in the session was done **without live-testing the Gemini-dependent path** — verify all of it once quota resets: check `https://ai.dev/rate-limit` for reset timing/current usage first
- [ ] **NEEDS TESTING (2026-09-08)** — Gemini-dependent, blocked by the quota above, not yet exercised live at all: re-verify `estimate_macros`, `estimate_recipe`, `create_custom_food` (text + image) still work end-to-end once quota resets — nothing in the underlying logic changed since the last verified-good state, but nothing new since the quota hit has been proven live either
- [ ] **Verify g-vs-ml unit choice for liquids once quota resets.** The system instruction (`src/lib/llm/estimate-macros.ts`) only explicitly rules on `"pcs"` — "discrete/countable items (eggs, slices, pieces)" — and leaves the `g` vs `ml` choice for everything else entirely to the model's own judgment; there's no explicit "liquids/beverages get ml" rule written anywhere. Untested whether this produces `ml` for something like "1 glass of orange juice" in practice. If it doesn't, add an explicit line to the instruction (e.g. "liquids/beverages get 'ml', solids/semi-solids get 'g'") — cheap fix, just needs a live check first rather than guessing it's needed

## UI polish

- [x] Global "Add Food" floating button now defaults to a meal based on the actual time of day (`mealForTime()` in `src/lib/mock-data.ts`: 5-11am Breakfast, 11am-2pm Lunch, 5-9pm Dinner, else Snacks) instead of always hardcoding "Breakfast". Per-meal-section add buttons untouched — they already pass their own meal explicitly. Verified: boundary hours + the actual current time all resolved correctly
- [x] Add Food/Recipe sheet "Describe it": textarea instead of single-line input (bigger, room for a real description), removed the caption text underneath both. Add Food's camera-attach button realigned to top since the box is no longer single-line-centered
- [x] Added a +/- quantity stepper to the Add Food sheet's Amount field — built as an opt-in `onStep`/`step` prop on the shared `NumericField` component rather than hardcoded, so Calories/Protein/Carbs/Fat (which don't want a stepper) render unchanged
- [x] "Recently logged" chips: were overflowing past the sheet's edge with no ellipsis for long names (e.g. "Bibingka with grated coconut") — added `max-w-40 truncate`, plus asymmetric `pl-3 pr-4` padding so the truncated text/ellipsis doesn't sit flush against the pill's rounded edge
- [x] **History day-detail view** — day cards on `/history` are now tappable, opening a sheet (`src/components/history/day-detail-sheet.tsx`, same mobile-bottom-sheet/desktop-dialog pattern as Add Food/Create Recipe) showing that day's actual logged foods grouped by meal. Made `MealSection`/`FoodRow`'s `onAdd`/`onDelete` optional instead of building a parallel read-only component — omitting them now renders a clean no-actions view (plain "Nothing logged" instead of "tap to add", no delete buttons). Verified live end-to-end: logged two entries across two meals, confirmed the History summary card's total matched, then confirmed the exact fetch the sheet makes (`GET /api/entries?date=`) returns both entries correctly split by meal
- [ ] Day-detail view is read-only by design (view only, matches what was actually asked) — no way to edit or delete a past day's entries from History yet, only from Today
- [x] **Day-detail sheet: added the actual date, and a progress ring/macro bars** — heading now shows "Monday, September 7" (parsed as UTC midnight, not local time, to match how entries are bucketed server-side — avoids an off-by-one day for users west of UTC) instead of just the weekday. Added the same `CalorieRing` + 3 `MacroBar`s Today uses (protein/carbs/fat), sized down for the sheet. Goals come from `useGoals()` (the same `GoalsProvider` Today reads from), not the summary endpoint's calorie-only figure, so per-macro goals are available too — confirmed `GoalsProvider` wraps History via `(app)/layout.tsx` before relying on it. Totals are computed from the same entries list rendered below (not a separately-fetched summary number) so the ring/bars and the list can't drift apart. Verified: the date formatter produces correct output for several dates including today's actual date, and a live goals+entries round trip (2000/150/200/65 goals, 600/40/50/20 logged) produces exactly the numbers the ring/bars would render
- [x] **Added food categories with icon + color** (`meat`/`seafood`/`dairy_eggs`/`fruit`/`vegetable`/`grain`/`snack`/`beverage`/`dessert`/`meal`/`other`) — new nullable `foods.category` enum column, pushed live. `src/lib/food-sources/categories.ts` is the single source of truth for the list + lucide icon + color per category (colors deliberately distinct from the app's macro colors so a badge never reads as a macro indicator). Threaded through all three creation paths: `customFoodSchema`/`createCustomFood` (manual + MCP `create_custom_food`), `estimate_macros`'s Gemini schema (new `category` field, classified alongside name/macros), and `estimate_recipe`'s per-ingredient schema. New shared `CategoryBadge` component (small colored icon circle, null falls back to the neutral "other" icon rather than an inconsistent gap) added to both search result lists (Add Food sheet, `FoodSearchInput`) and the recipe sheet's selected-ingredient cards
- [x] Verified live everything that doesn't require Gemini: created a custom food with `category: "fruit"` directly, confirmed it round-trips correctly through search, and confirmed an invalid category value is correctly rejected (400) by the zod enum validation
- [ ] **NEEDS TESTING (2026-09-08)** — Gemini-dependent, blocked by the still-active quota: the actual LLM auto-classification (does "Describe it"/`create_custom_food`/`estimate_recipe` produce sensible categories in practice — e.g. does "sinigang" correctly get `meal` rather than `meat`/`vegetable`) has not been exercised live at all yet, only the schema/storage/display plumbing around it
- [x] **Added a manual category picker** (`src/components/food-search/category-picker.tsx`) to the Add Food form — the catalog was empty and Gemini quota-blocked, so there was no way to actually see a non-"Other" badge without this. Row of tappable category chips, shown only when creating a genuinely new food (`!selectedFoodId` — an existing catalog pick already has its own category, nothing to set). Pre-filled from an LLM estimate when one applies, but stays independently editable — `form.category` is now the single source of truth passed to `POST /api/foods/custom`, replacing the earlier direct `lastEstimate?.category` reference. Verified live end-to-end (no Gemini involved): created a food with `category: "meat"` via the exact path the picker drives, confirmed it round-trips through search correctly
- [x] **Add Food: multi-food staging** — was one-food-per-sheet-open; now supports adding several in one visit. Confirmed the interaction model and card design with the user before building (staging list + one final submit; macro-composition ring + calories, one row per card — not progress-toward-goal). New "Add another" button resolves the current form (including the existing custom-food-creation step) into a compact staged card and clears the form for the next food; the main button becomes "Log N to {meal}" once anything's staged, and also resolves whatever's left in the form so the last item doesn't need an explicit "Add another" tap. New `MacroRing` component (`src/components/rings/macro-ring.tsx`) — a static 3-segment donut of protein/carbs/fat by calorie contribution (4/4/9 kcal per g), distinct from `CalorieRing`/`MiniRing` which show progress toward a goal, not composition. Verified live: staged 3 real foods (each with its own custom-food creation + category), submitted together, confirmed all 3 landed as separate entries under the same meal, each correctly linked to its own `foodId`
- [x] Staged-food card's macro display iterated twice on direct feedback after the above: the combined donut → 3 separate ring badges with P/C/F letters inside → finally plain color-coded text ("P32"/"C45"/"F12", no ring shape). `macro-ring.tsx` deleted, replaced by `macro-letter-badge.tsx` (now text-only, despite the filename)
- [x] **Fixed a real bug**: increasing Amount only rescaled macros for a search-picked food (which has a known per-100 catalog rate) — a hand-typed or "Describe it"-estimated food silently left its macros untouched when Amount changed, since there was no rate to scale from. Fixed `handleAmountChange` to fall back to scaling proportionally from whatever's currently in the macro fields when no catalog rate is known, instead of doing nothing. Verified the math directly: 100g→200g on a hand-typed 165kcal/31g-protein food correctly doubles to 330kcal/62g; a real catalog rate still rescales precisely (no accumulated rounding drift) as before
- [ ] **Category picker removed from the Add Food form UI, by request** — `src/components/food-search/category-picker.tsx` deleted (was fully unused after removal, trivial to recreate from git history if wanted back). The underlying category system is untouched and still fully functional: LLM auto-classification (`estimate_macros`/`estimate_recipe`), `CategoryBadge` display everywhere, the `foods.category` column/schema — a hand-typed food (with no LLM call involved) just has no way to manually set its category anymore, so it stays `null`/"Other". Revisit if manual categorization turns out to matter in practice

## Goals

- [x] Manual goal editing already existed (`/settings`, `GET`/`PUT /api/goals`) — verified still working before building anything new, no changes needed there
- [x] **Added a biometric goal calculator** — deliberately deterministic (Mifflin-St Jeor BMR × activity multiplier, ± a calorie offset for lose/maintain/gain, protein-per-kg + 28%-fat-of-calories + carbs-fill-the-rest for the macro split), not an LLM call, since it's pure arithmetic and sidesteps the still-active Gemini quota entirely. New `src/lib/goals/calculate-goals.ts` (`calculateGoals()`, plus `lbToKg`/`inToCm` unit helpers) is a pure, synchronous function with no DB/secrets, so both callers use it directly with no API round-trip
- [x] Settings UI: new collapsible "Calculate with AI" panel (`src/components/goals/goal-calculator.tsx`) under the existing goal fields — sex, age, weight/height (metric or imperial toggle), activity level (5 options), goal direction (lose/maintain/gain). "Calculate Goals" fills the existing calorie/protein/carbs/fat fields (doesn't auto-save) so the user still reviews and hits the existing "Save Goals" button — reuses the manual-edit save path rather than adding a second one
- [x] **Added a sixth MCP tool, `calculate_goals`** — same shared `calculateGoals()`, so a Claude conversation gets identical numbers to the web calculator. Takes the same biometric inputs (tool description tells the model to convert lb/ft-in to kg/cm itself rather than building conversion into the schema); by default also saves the result as the active goal (same `insert...onConflictDoUpdate` on `macroGoals` the `/api/goals` PUT route already uses) so "set my goals based on my stats" works in one call — `save: false` previews without writing
- [x] Verified live: hand-derived the expected BMR/TDEE for a 30yo male/80kg/180cm/moderate/maintain case (1780 BMR × 1.55 = 2759 kcal) and confirmed both the raw function and the running MCP server (real stdio protocol call, `tools/list` showing all 6 tools) return exactly that; confirmed the Settings page renders the new calculator (registered a throwaway test account, fetched the authenticated page, found the button in the rendered HTML). Deliberately did *not* live-test the MCP tool's `save: true` default against the real account (`MCP_USER_EMAIL` resolves to the actual single user, not a test account) — the write path is identical to the already-proven `/api/goals` route, so this was judged safe to skip rather than overwrite real saved goals for a test
- [ ] No biometrics (weight/height/age/sex/activity level) are persisted anywhere — each calculation is a one-off input, not saved to a profile. Revisit if re-calculating without re-typing stats turns out to matter in practice
- [x] **AI-branding pass + Today quick-edit, per direct feedback**: kept the existing Sparkles icon for AI features (no change there), but the goal calculator's trigger went from a plain text link to a proper filled pill button ("Calculate with AI") — explicitly generic (Sparkles, not a model-specific logo) since the user is about to share this with friends and doesn't want the underlying provider (Gemini) identified in the UI. Extracted the Settings "Daily Goals" card's fields + Save/Reset + calculator into a shared `src/components/goals/goal-editor.tsx` (moved `goal-calculator.tsx` from `settings/` to a new `goals/` dir alongside it) so it could be reused rather than duplicated. Added a pencil "Edit goals" button (top-right, on the Today page's macro-ring card) opening a new `EditGoalsSheet` (`src/components/goals/edit-goals-sheet.tsx`, same bottom-sheet/desktop-dialog shell as `DayDetailSheet`) that renders the identical `GoalEditor` — so goals (manual or AI-calculated) can now be edited directly from Today, not just Settings
- [x] Verified live: typecheck + lint clean (one pre-existing, unrelated lint warning on Today's `loadEntries` effect, not touched by this change); registered a throwaway test account and confirmed both `/` and `/settings` render 200 with the new "Calculate with AI" text present in Settings' authenticated HTML (Today's edit button is inside the client-fetched `loading` branch, so it doesn't show in raw SSR HTML the same way `CalorieRing` already didn't — not a regression, just how that page has always loaded). Test account cleaned up afterward
- [x] **Calculate-with-AI panel: swapped Sex/Units/Goal from `SegmentedControl` toggles to plain `<select>` dropdowns**, per direct feedback for a more minimal look — matches the Activity level field, which was already a dropdown. Extracted a small local `Dropdown<T>` helper within `goal-calculator.tsx` (label + `<select>`, optional value→label map for Activity level's longer descriptions) instead of repeating the same select markup four times. `SegmentedControl` import removed — no longer used in this file. Purely presentational, no behavior change; typecheck/lint clean
- [ ] DB latency/connection-pooling investigation (neon-http, ~795ms per route) — explicitly deferred earlier in favor of this feature, still open
